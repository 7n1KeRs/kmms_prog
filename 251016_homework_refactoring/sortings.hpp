#ifndef SORTINGS_HPP
#define SORTINGS_HPP

namespace biv{
	namespace sortings{
		void merge_sort (int* arr, int left, int mid, int right);
	}
}

namespace biv{
	namespace sortings{
		void print_merge_sort (int* arr, int left, int right);
	}
}

#endif